{
  "name": "bridle",
  "private": true,
  "version": "0.1.0",
  "description": "Tailscale for agents \u2014 an open protocol for agent-to-agent handoff",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "npm run build && node --test packages/*/dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^5.9.2"
  }
}